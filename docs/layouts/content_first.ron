// Keeps the default top/bottom shell, but makes the library content the first
// thing in the main row and pushes the navigation sidebar to the far right.
(
  outer_order: (
    header,
    main,
    controls,
  ),
  main_order: (
    library_content,
    right_sidebar,
    library_sidebar,
  ),
)
